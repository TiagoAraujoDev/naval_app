import { db } from "../database/knex.js";

class PermissionController {
  async add(req, res) {
    // TODO: Add a permission to a role
  }

  async delete(req, res) {
    // TODO: Remove a permission to a role
  }
}

export { PermissionController };
